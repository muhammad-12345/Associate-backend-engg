// const admin = require('./src/config/firebase'); 

// const generateCustomToken = async (uid) => {
//     try {
//         const customToken = await admin.auth().createCustomToken(uid);
//         console.log('Custom Token:', customToken);
//     } catch (error) {
//         console.error('Error creating custom token:', error.message);
//     }
// };

// // Simulate a user ID for testing purposes
// const testUID = 'user123'; // Replace with a valid UID if available
// generateCustomToken(testUID);